import React from "react";
import ReactDOM from "react-dom";

const title = (
  <div>
    <h1>hello react use jsx</h1>
    <span>这是span</span>
  </div>
);

ReactDOM.render(title, document.getElementById("root"));
